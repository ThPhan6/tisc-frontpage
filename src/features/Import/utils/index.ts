export const generateWarehouseName = (index: number) => ({
  key: `#${index} Warehouse name`,
  label: `#${index} Warehouse name`,
});

export const generateWarehouseInStock = (index: number) => ({
  key: `#${index} Warehouse In Stock`,
  label: `#${index} Warehouse In Stock`,
});
